
Activate miniforge
    eval "$(~/miniforge3/bin/conda shell.bash hook)"

Load environment
    conda activate gradual-aacbr


Queue job with parameter:
    qsub -v SWEEP_ID="<SWEEP_ID>" HPC/wandb_sweep_template.pbs 

Check queue status:
    qstat
    qstat -u amg2118

Check job status
    qstat -f <JOB_ID>

Delete job
    qdel <JOB_ID>