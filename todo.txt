TODO:

###    0. Refactor MNIST example
###    1. Ensure the order we pass X_casebase, X_default and X_new_case is consistent
###    2. Make sure both train functions take in the same arguments so that we can swap them out
    3. Add type annotations and doc strings
###    4. Change the gradual aacbr fit functions to be consistent with each other
###    5. Ensure we are passing "use_blockers" to all necessary functions and in these functions, they all have the same default value
    6. Fix imports so they get reloaded correctly in notebooks
    7. Put clustering into it's own module in the deeparguing package - ensure it has a consistent interface so we can try different clustering methods
    8. Put normalizing data as a helper function
    9. Put baseline testing into a separate examples file
###    10. Reorganize examples directory to have subdirectories by dataset 
    11. Separate hyperparameter training and running the model into separate notebooks for each dataset
    12. Add batching when constructing the AF from the casebase so we can use larger casebases